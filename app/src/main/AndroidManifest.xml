<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bilalmoreno.malagasport">

    <!-- Permisos necesarios para obtener la ubicación del usuario cuando se implemente el Mapa -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- API Key de Google Maps para este proyecto -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="@string/google_maps_key" /> -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.gms.version" -->
        <!-- android:value="@integer/google_play_services_version" /> -->

        <activity android:name=".ui.DashBoardActivity" />
        <activity android:name=".ui.Login.LoginActivity" />
        <activity android:name=".ui.workout.WorkoutActivity" />
        <activity android:name=".ui.Login.RegisterActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.Login.RecuperarActivity" />
        <activity android:name=".ui.workout.MachineListFragment" />
        <activity android:name=".ui.SettingsFragment" />
        <activity android:name=".ui.installation.InstallationActivity" />
    </application>

</manifest>